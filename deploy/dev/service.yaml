apiVersion: v1
kind: Service
metadata:
  name: whereabouts-api-svc
  namespace: whereabouts-dev
spec:
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  type: NodePort
  selector:
    app: whereabouts-api

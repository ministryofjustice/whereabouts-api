elite2api:
  client:
    clientId: whereabouts-api-client
    clientSecret: clientsecret
    accessTokenUri: http://localhost:8090/auth/oauth/token
    tokenName: elite2-token
    authenticationScheme: header
    clientAuthenticationScheme: header
  endpoint.url: http://localhost:8999
  host: localhost
  port: 8999

casenotes:
  endpoint.url: http://localhost:8093

oauth:
  endpoint:
    url: http://localhost:8090/auth

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:local-public-key.pub

sqs:
  provider: localstack
  endpoint.url: http://localhost:4566
  queue.name: whereabouts_api_queue
  dlq.name: whereabouts_api_dlq


courts: Test Court 1,Test Court 2

server:
  shutdown: immediate
